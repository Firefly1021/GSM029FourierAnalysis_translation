$2. Function Spaces for the Stokes Problem
31
pe
curl o
x2
0x1
002
001
curlv =
0x1
0x2
When N = 3, we define the curl of a distribution v of D'(Q) by
(003
0v2001
0v3002
0v1
curlv :
0x2
0x'0x30x'0x 
0x2,
It can be easily checked that the following identities hold:
curl(curl Φ) = - Φ  N = 2,
curl(curl v))
N = 3,
-- 4v + grad(div v).
curl(curl v))
N = 2
Note that the curl of a two-dimensional vector field is a scalar. In order to avoid
a multiplicity of notation, we agree nevertheless to denote it like a vector,
provided there is no confusion.
To begin with, we establish an extension of the following classical Stokes'
Theorem:
if a function of class C1 has a vanishing curl in a simply-connected region of
R', then this function is a gradient.
Theorem 2.9. Assume in addition that Q is simply-connected. A function u of
L²(Q) satisfies:
curlu = 0 in Ω
iff there exists a unique function p of H'(Q)/R such that:
u = grad p.
Proof. The proof is very similar to that of Theorem 2.3: we first prove that
u = grad p for some p in L?c(Ω) and we apply Corollary 2.2 to show that
pe L2(Ω). Obviously pe H(Q) and is uniquely defined in H'(Q)/R.
Here again, we can find an increasing sequence of simply-connected, Lipschitz-
continuous open sets (Ωm)m≥1 such that:
Ωm cΩ and Ω = U Ωm
m≥1
The idea is to regularize u so that its restriction to Qm has a vanishing curl. Then
we shall be able to apply Stokes' Theorem to this smooth function.
For & > 0, let pε (R) be the mollifier introduced in Theorem 2.3 and let
u denote the extension of u by zero outside Q. Then
