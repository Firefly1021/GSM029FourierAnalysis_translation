§5. The Stokes Equations
87
a,(u,(q),v) = <f,v) + (divv,q)
N()HAA
u,(q) =g on I,
we find in view of (5.23):
a,(Du, · μ,v) = (div v, μ)
Wve H(Q),
Du,'μ =O on F,
DK,(q) = div u,(q),
D² K,· μ = div(Du, · μ).
Hence the simple gradient algorithm with optimal parameter reads as follows:
1°) Given p°e L?(Ω), solve the non-homogeneous elliptic boundary value
problem:
-(v4 + rgrad div)u° = f - grad p° in Ω,
u° =g on I.
2°) For m ≥ 0, knowing (u", pm) in Hl(Q) × L2(Ω) with umlr = g, solve the
homogeneous problem:
-(v4 + r grad div)zm = grad div um  in Ω,
z" =0 on I;
then compute pm e R and the next pair (um+1, pm+1)e H′(Q) x L?(Ω) by:
pm = - I/ div um Il?,2/(div zm, div um),
pm+1 = pm -- pmdivum,
um+1 = um + pmzm.
It can be readily checked that the statement of Theorem 4.6 holds, namely the
above scheme is always convergent. This is still true when p is not the optimal
parameter, provided that
0 < infpm ≤ sup pm < 2αr.
m
m
A simple calculation shows that
a, ≤r + v/N.
The conjugate-gradient algorithm has the same starting procedure n?°1 while
step n°2 is replaced by:
2°) For m ≥ 0, knowing (um, pm) in H1(Ω) × L(Ω) with umlr = g, compute
om e R and wm e L?(Ω) by:
gm =1ldiv um/0.2/ div um-1 1.2,
only if m ≥ 1,
@m = divum + om@m-1
①° = div u° otherwise,
