38
I. Mathematical Foundation of the Stokes Problem
2°) Conversely, let v satisfy (3.1). The idea is to extend v to the whole plane
so that it remains divergence-free. Then, it will be easy to construct its stream
function by means of the Fourier transform.
a) We propose to use an extension that has the same normal component as
v on I.
Let O be any bounded, smooth, simply-connected open set with Ω c O. Then,
for p ≥ 1, the set 0 - Ω is not connected and we denote by Ω, that component
which is bounded by F;, for 1 ≤i ≤ p, and Ω。 that component bounded by Io
and dO (cf. Figure 2). Consider the following problem:
Find w defined in O - Q such that
4w = 0 in ( -Ω,
()
 ow/on = v·n on Ii for O ≤i ≤ p,
dw/dn = 0 on 00,
where n denotes the unit normal to I; pointing outside Ω, 0 ≤ i ≤ p. Problem
(N) consists of p + 1 non-homogeneous Neumann's problems, each defined in
Q, for 0 ≤ i ≤ p, like the one we analyzed in Section 1.4 since they include the
compatibility conditions
<v·n, 1>r, = 0 for 0 ≤i ≤p.
Therefore, there exists a function w e H'(O -- Q) satisfying (N), determined unique-
ly up to an additive constant in each Ω. We set
0 = grad w.
Then 0e H(div; 0 - Ω) with
div0 = 4w = 0 in 0 - Ω,
(3.3)
0·n = dw/on = v·n on I; forO≤i≤p,
O·n = 0 on 00.
b) Now we can extend v as follows:
V
in Ω,
0
in O - Ω,
0
elsewhere.
Clearly, ve L?(R2)2. Let us calculate its divergence; as a distribution, div  satisfies:
<div,Φ>=
·grad Φ dx
()G中A
JR2
that is
<divv,Φ> =
V: grad Φ dx
O· grad Φ dx.
=0Jo
As v and 0 are both divergence-free, Green's formula (2.17) and (3.3) yield:
