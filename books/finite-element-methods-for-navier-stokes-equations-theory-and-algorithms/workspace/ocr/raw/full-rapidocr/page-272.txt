258
I11. Incompressible Mixed Finite Element Methods for Solving the Stokes Problem
As divu = O we obtain first:
v<curl curl u, curl Φ> = (f, curl Φ)
()HA(
where < ., . > denotes the duality between Ho(curl; Ω) and its dual space. Now, let
us set
(5.3)
① = curl u
and assume that o e H(curl; Ω); then we have
v(curl o, curl Φ) = (f, curl Φ)
()HA
Finally, since
u = curly with div y = O and yeY,
equation (5.3) can be written equivalently as:
(curl curl w,p) = (o,μ)
E()TA
And by restricting μ to H(curl; Ω) this becomes:
(curl w, curl μ) = (@,μ)
()HA
Hence, summing up we see that the following Problem (Q):
Find a pair (y,?)e Ho(curl; Q) x H(curl; Q) such that:
(5.4)
v(curl o, curl Φ) = (f, curl Φ)Φ e Ho(curl; Ω),
(5.5)
(curl y, curl μ) = (o, p)
()HA (
(5.6)
divv = 0,
has at least one solution (w, w = -- 4w) where u = curl y and y e Y. Conversely,
it is easy to check that this problem has at most one solution. Indeed, if
(curl w, curl Φ) = 0  VΦ ∈ Ho(curl; Ω),
 0 = o Aeaissons io m ('s) ui m = n pue  =  susoouo u
curl y = 0. Since div y = O and F has only one connected component this implies
that y = 0 (cf. Remark 1.3.9). Therefore, we have proved the following result:
Theorem 5.1. Assume that the solution u of Problem (5.1) satisfies
curl ue H(curl; Ω).
Then Problem (5.4) (5.5) (5.6) has the unique solution:
(W, - 4w) where curl y = u.
Now we want to insert Problem (Q) into the framework of Section 1.1. We set
Do = {Φe Ho(curl; Ω); div Φ = 0}.
Clearly, Problem (Q) is equivalent to:
