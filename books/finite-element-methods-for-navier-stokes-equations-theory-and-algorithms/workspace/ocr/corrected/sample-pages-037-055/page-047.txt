Lemma 2.3. Suppose Q is a Lipschitz-continuous open subset of RN (not necessarily
bounded). Then the set of functions of H(curl;Q) with compact support is dense in
HA (curl; Q).
Proof. We use a classical truncation process. Let ue H(curl; Q) and let ¢ be a
function of @*(R”) such that 0 < ¢ < 1 everywhere in R™ and
ree : for ||x|| <1,
0 for ||x|| > 2.
Next, for an arbitrary real number a > 0, we make the change of variable:
ba(X) = $(x/d).
As ¢, is smooth, ¢,u€ H(curl; 2); moreover
lim g,u=u_ in H(curl; Q)
a>o
and the support of ¢,u is compact. Hence ¢,u is the desired sequence. O
Lemma 2.4. If a function f of H(curl; Q) satisfies:
(2.21) (f, curl @) — (curlf,d)=0 VbeZ(Q)%,
then f € H,(curl; 2).
Proof. Let f denote the extension of f by zero outside Q. To begin with, observe
that (2.21) implies that fe H(curl; R®). Next, in order to construct a sequence of
GQ)" that converges to fin H(curl; Q), we combine the regularization techniques
of Corollary 2.2 and Theorem 2.3.
1°) Suppose for the moment that Q is strictly star-shaped. Again, we make
the change of variable introduced in Corollary 2.2:
f,(x) = f(x/0) VxeQ, = OQ,
but this time, we take 0 in (0, 1) in order to obtain a function f, with compact
support in 2. Obviously, f,¢ H(curl; R*) and
limf, =f in H(curl;R%).
6-1
Hence, for ¢ > 0 sufficiently small, p, « f, belongs to (Q)” and
lim lim (p, * f,) = f in H(curl; Q).
é70 6-1
2°) In the general case, 2 can be covered by a finite family of open sets:
er liecd;
1<i<q
such that each Q, = ©; Q is Lipschitz-continuous, bounded and strictly star-
shaped, for 1 <i < q. Let («;);5, be a partition of unity subordinate to the family
