# Frontmatter preface translation QA

Status: passed.

- Translation was made from the scan-verified reconstructed English source, not from raw OCR.
- No source paragraph was omitted or duplicated.
- Chinese prose uses ASCII half-width punctuation only.
- All personal names found on PDF pages 9--10 are present exactly as recorded in `glossary/proper-names.tsv`.
- The English names `Theorem`, `Definition`, `Lemma`, `Proposition`, `Corollary`, `Proof`, `Example`, `Remark`, and `Exercise` are not involved in this unit and were not translated as environment headings.
- The formulas $H(\operatorname{div};\Omega)$ and $H(\operatorname{curl};\Omega)$ are unchanged.
- References to the four chapters use semantic LaTeX labels rather than hard-coded chapter numbers.

All four chapter references now resolve to the reviewed semantic labels
`chap:stokes-foundations`, `chap:stokes-primitive-variables`,
`chap:incompressible-mixed-stokes`, and `chap:navier-stokes-theory-approx`.
The table of contents is generated from the reviewed headings and is not duplicated as manually entered body text.
