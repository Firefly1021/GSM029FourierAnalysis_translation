# Chapter 1, Section 1, Part 1 formula report

- Scope: PDF pages 15--18 / printed pages 1--4.
- Formula regions registered in the full registry: 56.
- Numbered equations in this range: (1.1)--(1.6).
- Semantic labels created: `eq:sobolev-norm-mp`, `eq:sobolev-seminorm-mp`, `eq:sobolev-hm-inner-product`, `eq:poincare-friedrichs`, `eq:negative-sobolev-dual-norm`, and `eq:sobolev-conjugate-exponent`.
- The reference from Lemma 1.2 to source equation (1.6) is implemented as `\eqref{eq:sobolev-conjugate-exponent}`.
- Direct scan corrections to auxiliary recognition include `mathcal O`, alpha indices, the conjugate exponent prime, `C^{m-1,1}`, and `Gamma` in the distance function.
- Possible source inconsistency: the first displayed restriction formula on printed page 1 visibly has `D(Omega)`, whereas the following equivalent display has `D(bar Omega)`. The translation preserves both source forms; issue `SCAN-CH1-S1-SOURCE-001` records the observation.
- Low-confidence critical formulas after review: 0.
- Status: passed for this partial range.
