# Sample cross-reference report

- Semantic labels: 5.
- Duplicate labels: 0.
- Resolved local reference commands: 1.
- Undefined LaTeX references after three XeLaTeX passes: 0.
- Multiply-defined labels: 0.
- Rerun warnings: 0.
- Hard-coded local numbers in translated prose: 0.
- Rendered numbers: chapter 6, section 6.3, figure 6.1, definition 6.3.1 and equation 6.3.2.

Five source references target objects outside the selected sample. They are recorded with stable semantic target keys and `\MBUnresolvedReference`, without guessing or creating false anchors. When the preceding and following sections are translated, equation targets must use `\eqref` and theorem-like targets must use `\ref`.

Result: passed for an isolated representative sample, with five non-blocking external targets pending whole-chapter integration.
