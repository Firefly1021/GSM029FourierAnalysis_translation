# Sample terminology report

- Authoritative global terminology library queried: `glossary/terminology.tsv`.
- Matching approved entries: 0; the current global library contains no approved rows.
- Book-specific candidates added: 3.
- Book-specific unresolved entries added: 1.
- Existing global terminology rows modified: 0.

The unresolved choice is `scheme`: the sample uses `格式`; `方案` remains an alternative requiring user review.

Result: passed with one nonblocking unresolved term.
